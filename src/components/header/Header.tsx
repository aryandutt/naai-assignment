import React from "react";
import HeaderBackButton from "./HeaderBackButton";

const Header = () => {
  return (
    <div className="px-4 py-8 shadow-lg shadow-slate-100">
      <HeaderBackButton name="Shop hours" />
    </div>
  );
};

export default Header;
